//SCOPES
function lol() {
  let person = 'Tom';
  const age = 45;
  var color = 'teal';
}

//accessing outside of function wont work
//can have the same variable if one is in and one is outside of the function
